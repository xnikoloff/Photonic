@model List<GalleryPhoto>

<div class="container mt-5 py-5">
    <div class="row bg-light my-3 p-3 rounded">
        <div class="col-12 col-md-6 col-lg-12">
            <h4>@(ViewData["categoryDescription"] ?? "")</h4>
        </div>
    </div>
    <partial name="_Grid" model="Model"/>
</div>